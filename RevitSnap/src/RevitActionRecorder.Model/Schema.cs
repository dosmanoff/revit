namespace RevitActionRecorder.Model;

/// <summary>
/// Версия JSON-схемы всех выходных файлов (manifest, snapshot, events, diff).
/// Любое несовместимое изменение формата — инкремент версии.
/// </summary>
public static class Schema
{
    /// <summary>
    /// 1 — исходная схема.
    /// 2 — метаданные параметров вынесены из каждого элемента в общий словарь `paramDefs`
    ///     (имя/bip/guid/тип хранения/спецификация повторялись в каждом из миллионов параметров
    ///     и составляли около половины файла); у элемента остаются только pid и значения.
    ///     В событиях у изменённых элементов появилась дельта положения `loc`.
    /// </summary>
    public const int Version = 2;
}
