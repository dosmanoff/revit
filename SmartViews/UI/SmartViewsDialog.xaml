<Window x:Class="SmartViews.UI.SmartViewsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SmartViews — Configure Views"
        Width="540" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Margin" Value="0,4,8,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="Padding" Value="4,2"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="0,4,0,0"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="16,4"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Config folder -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Config folder:"/>
            <TextBox Grid.Column="1" x:Name="TxtConfigFolder"/>
            <Button Grid.Column="2" Content="…" Click="BrowseFolder_Click" Margin="4,4,0,0"/>
        </Grid>

        <!-- Crop offset -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Crop offset (ft):"/>
            <TextBox Grid.Column="1" x:Name="TxtCropOffset"/>
        </Grid>

        <!-- Duplicate handling -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="Duplicates:"/>
            <ComboBox Grid.Column="1" x:Name="CmbDuplicates" Margin="0,4,0,0"/>
        </Grid>

        <!-- View kinds list header -->
        <TextBlock Grid.Row="3" Text="View kinds to create:" Margin="0,12,0,4" FontWeight="SemiBold"/>

        <!-- View kinds data grid -->
        <DataGrid Grid.Row="4"
                  x:Name="GridViewKinds"
                  AutoGenerateColumns="False"
                  CanUserAddRows="True"
                  CanUserDeleteRows="True"
                  MinHeight="120"
                  MaxHeight="300"
                  Margin="0,0,0,12"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridComboBoxColumn Header="Kind"
                                        SelectedItemBinding="{Binding Kind}"
                                        Width="110"/>
                <DataGridTextColumn Header="Name template"
                                    Binding="{Binding NameTemplate}"
                                    Width="*"/>
                <DataGridTextColumn Header="View family type (optional)"
                                    Binding="{Binding ViewFamilyTypeName}"
                                    Width="160"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" IsDefault="True" Click="Ok_Click"/>
            <Button Content="Cancel" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
